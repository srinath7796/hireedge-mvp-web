export default function SkillsPage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>AI Skills-Matching Engine</h1>
      <p>Here we will call /api/skills-matching on the backend and show fit score & gaps.</p>
    </main>
  );
}
